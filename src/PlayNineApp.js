import React from 'react';
import ReactDOM from 'react-dom';

const Stars = (props) => {
    return (
        <div>
        </div>
    );
};

export default class PlayNineApp extends React.Component {

    render() {
        return (
            <div>
                <h3>Play Nine</h3>
            </div>
        );
    }
}

ReactDOM.render(<PlayNineApp />, document.getElementById('root'));